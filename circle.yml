machine:
  timezone:
    Asia/Tokyo
  node:
    version: 5.0.0

dependencies:
  override:
    - npm run init

test:
  override:
    - exit 0

deployment:
  production:
    branch: master
    commands:
      - git config --global user.name "ygoto3"
      - git config --global user.email "my.important.apes@gmail.com"
      - npm run update
      - npm run build
      - npm run deploy
